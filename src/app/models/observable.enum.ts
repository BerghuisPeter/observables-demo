export enum OBSERVABLE_ENUM {
  normal = 'normal',
  tap = 'tap',
  map = 'map',
  concat = 'concat',
  concatMap = 'concatMap',
  merge = 'merge',
  mergeMap = 'mergeMap',
  switchMap = 'switchMap',
  forkJoin = 'forkJoin',
  retry = 'retry',
  catchError = 'catchError'
}
